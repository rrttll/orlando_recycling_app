//File: lib/screens/map_schedule_screen.dart
import 'package:flutter/material.dart';

class MapScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Map & Schedule")),
      body: Center(child: Text("This is the Map & Schedule Screen")),
    );
  }
}
